/**
 * pages/3_평가시스템.py 대응 예정 페이지.
 * 기존 기능(체크포인트 저장/자동 이어하기·GPT 상태 가드·CSV 다운로드)을 동일하게
 * 유지하기로 했으므로, 이 페이지를 채우기 전에 백엔드에 비동기 작업 실행 API
 * (작업 시작 → job id 발급 → 진행률 폴링/SSE)를 먼저 추가해야 한다 — 지금의
 * "브라우저가 열려 있는 동안 스트림릿 프로세스가 반복문을 돌리는" 방식은
 * SPA에서는 그대로 쓸 수 없기 때문. 아직 백엔드 작업이 선행되지 않아 준비 단계로 남겨둔다.
 */
export default function EvalSystem() {
  return (
    <div>
      <h1>평가시스템 (준비 중)</h1>
      <p style={{ color: 'var(--text-dim)' }}>
        백엔드에 비동기 배치 작업 API가 먼저 필요합니다 — 아직 미착수.
      </p>
    </div>
  )
}
